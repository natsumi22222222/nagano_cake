<h5>注文時情報確認</h5>
<%= form_with model: @order, url: orders_complete_path do |f| %>
  <table>
    <thead>
      <tr>
        <th>商品名</th>
        <th>単価（税込）</th>
        <th>数量</th>
        <th>小計</th>
      </tr>
    </thead>

    <tbody>
      <%= @cart_item.each do |cart_item| %>
      <tr>
        <td><%= image_tag item.name item.name %></td>
        <td><%= item.with_tax_price %></td>
        <td><%= item.amount %></td>
        <td><%= item.subtotal %></td>
      </tr>
      <% end %>
    </tbody>
  </table>

  <table>
    <tr>
      <th>送料</th>
      <td>800</td>
    </tr>
    <tr>
      <th>商品合計</th>
      <td><%= @total %></td>
    </tr>
    <tr>
      <th>請求金額</th>
      <td><%= @total + 800 %></td>
    </tr>
  </table>
<% end %>

<p>支払方法</p>
<p>お届け先</p>

