<p> 商品一覧（全<%= @count %>件）</p>

<% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
    <%= image_tag item.image, size: "100x100" %>
  <% end %>
    <p><%= item.name %></p>
    <p>¥<%= item.with_tax_price %>(税込)</p>
<% end %>
