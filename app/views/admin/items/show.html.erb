<p>商品詳細</p>

<table>
  <tbody>
    <th><%= image_tag @item.image, size: "300x300" %></th>
    <th>商品名</th>
    <td><%= link_to @item.name, edit_admin_item_path(@item.id) %></td>
    <th>商品説明</th>
    <td><%= @item.introduction %></td>
    <th>ジャンル</th>
    <td><%= @item.genre_id %></td>
    <th>税込価格（税抜価格）</th>
    <%= @item.price %>
    <th>販売ステータス</th>
    <td><%= @item.is_active %></td>

  </tbody>
</table>
